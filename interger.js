const prompt = require("prompt-sync")();

const Number = parseInt(prompt("Please enter a number"))

if (Number >= -1)
    {
        console.log(" You've enter a positive number")
    }

else

    {
        console.log("You've enter a negative number")


    }